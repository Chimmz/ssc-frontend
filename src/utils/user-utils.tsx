import { Session } from 'next-auth';

import useSignedInUser from '../hooks/useSignedInUser';

// type User = ReturnType<typeof useSignedInUser>['user'];

// const getFullname = (user: User | undefined) => {
//   return user?.name
// }
